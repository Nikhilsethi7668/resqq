#!/usr/bin/expect -f
set timeout 300
spawn ssh root@66.94.120.78
expect {
  "yes/no" { send "yes\r"; exp_continue }
  "password:" { send "Klal@5525\r" }
}
expect -re "\[#$]"
send "cd resqq\r"
expect -re "\[#$]"
send "git pull\r"
expect -re "\[#$]"
send "docker compose up -d --build backend\r"
expect -re "\[#$]"
send "ufw status\r"
expect -re "\[#$]"
send "netstat -tuln | grep 7003\r"
expect -re "\[#$]"
send "exit\r"
expect eof

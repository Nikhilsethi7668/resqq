import {
  X
} from "./chunk-V23SHEIM.js";
import {
  require_jsx_runtime
} from "./chunk-LVKE5TM5.js";
import {
  axios_default
} from "./chunk-6Z3TQB6K.js";
import {
  lookup
} from "./chunk-UFXOO3NO.js";
import {
  require_react
} from "./chunk-QRHVR3F3.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// ../shared/src/components/Button.jsx
var import_react = __toESM(require_react());
var import_jsx_runtime = __toESM(require_jsx_runtime());
import "/Users/ishananand/Desktop/resqq/frontend/shared/src/components/Button.css";
var Button = ({
  children,
  variant = "primary",
  size = "md",
  fullWidth = false,
  disabled = false,
  loading = false,
  icon = null,
  iconPosition = "left",
  onClick,
  type = "button",
  className = "",
  ...props
}) => {
  const baseClass = "btn";
  const variantClass = `btn-${variant}`;
  const sizeClass = `btn-${size}`;
  const widthClass = fullWidth ? "btn-full-width" : "";
  const loadingClass = loading ? "btn-loading" : "";
  const disabledClass = disabled ? "btn-disabled" : "";
  const classes = [
    baseClass,
    variantClass,
    sizeClass,
    widthClass,
    loadingClass,
    disabledClass,
    className
  ].filter(Boolean).join(" ");
  return (0, import_jsx_runtime.jsxs)(
    "button",
    {
      type,
      className: classes,
      onClick,
      disabled: disabled || loading,
      ...props,
      children: [
        loading && (0, import_jsx_runtime.jsx)("span", { className: "btn-spinner" }),
        !loading && icon && iconPosition === "left" && (0, import_jsx_runtime.jsx)("span", { className: "btn-icon btn-icon-left", children: icon }),
        (0, import_jsx_runtime.jsx)("span", { className: "btn-content", children }),
        !loading && icon && iconPosition === "right" && (0, import_jsx_runtime.jsx)("span", { className: "btn-icon btn-icon-right", children: icon })
      ]
    }
  );
};
var Button_default = Button;

// ../shared/src/components/Card.jsx
var import_react2 = __toESM(require_react());
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
import "/Users/ishananand/Desktop/resqq/frontend/shared/src/components/Card.css";
var Card = ({
  children,
  variant = "default",
  padding = "md",
  hoverable = false,
  glass = false,
  className = "",
  ...props
}) => {
  const baseClass = "card";
  const variantClass = `card-${variant}`;
  const paddingClass = `card-padding-${padding}`;
  const hoverableClass = hoverable ? "card-hoverable" : "";
  const glassClass = glass ? "card-glass" : "";
  const classes = [
    baseClass,
    variantClass,
    paddingClass,
    hoverableClass,
    glassClass,
    className
  ].filter(Boolean).join(" ");
  return (0, import_jsx_runtime2.jsx)("div", { className: classes, ...props, children });
};
var CardHeader = ({ children, className = "", ...props }) => {
  return (0, import_jsx_runtime2.jsx)("div", { className: `card-header ${className}`, ...props, children });
};
var CardBody = ({ children, className = "", ...props }) => {
  return (0, import_jsx_runtime2.jsx)("div", { className: `card-body ${className}`, ...props, children });
};
var CardFooter = ({ children, className = "", ...props }) => {
  return (0, import_jsx_runtime2.jsx)("div", { className: `card-footer ${className}`, ...props, children });
};
Card.Header = CardHeader;
Card.Body = CardBody;
Card.Footer = CardFooter;
var Card_default = Card;

// ../shared/src/components/Modal.jsx
var import_react3 = __toESM(require_react());
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
import "/Users/ishananand/Desktop/resqq/frontend/shared/src/components/Modal.css";
var Modal = ({
  isOpen,
  onClose,
  title,
  children,
  size = "md",
  showCloseButton = true,
  closeOnOverlayClick = true,
  className = "",
  ...props
}) => {
  const modalRef = (0, import_react3.useRef)(null);
  (0, import_react3.useEffect)(() => {
    const handleEscape = (e) => {
      if (e.key === "Escape" && isOpen) {
        onClose();
      }
    };
    if (isOpen) {
      document.addEventListener("keydown", handleEscape);
      document.body.style.overflow = "hidden";
    }
    return () => {
      document.removeEventListener("keydown", handleEscape);
      document.body.style.overflow = "unset";
    };
  }, [isOpen, onClose]);
  const handleOverlayClick = (e) => {
    if (closeOnOverlayClick && e.target === e.currentTarget) {
      onClose();
    }
  };
  if (!isOpen) return null;
  const sizeClass = `modal-content-${size}`;
  return (0, import_jsx_runtime3.jsx)("div", { className: "modal-overlay", onClick: handleOverlayClick, ...props, children: (0, import_jsx_runtime3.jsxs)("div", { className: `modal-content ${sizeClass} ${className} animate-scale-in`, ref: modalRef, children: [
    (title || showCloseButton) && (0, import_jsx_runtime3.jsxs)("div", { className: "modal-header", children: [
      title && (0, import_jsx_runtime3.jsx)("h2", { className: "modal-title", children: title }),
      showCloseButton && (0, import_jsx_runtime3.jsx)("button", { className: "modal-close", onClick: onClose, "aria-label": "Close", children: (0, import_jsx_runtime3.jsx)(X, { size: 20 }) })
    ] }),
    (0, import_jsx_runtime3.jsx)("div", { className: "modal-body", children })
  ] }) });
};
var ModalFooter = ({ children, className = "", ...props }) => {
  return (0, import_jsx_runtime3.jsx)("div", { className: `modal-footer ${className}`, ...props, children });
};
Modal.Footer = ModalFooter;
var Modal_default = Modal;

// ../shared/src/components/Input.jsx
var import_react4 = __toESM(require_react());
var import_jsx_runtime4 = __toESM(require_jsx_runtime());
import "/Users/ishananand/Desktop/resqq/frontend/shared/src/components/Input.css";
var Input = (0, import_react4.forwardRef)(({
  type = "text",
  label,
  error,
  helperText,
  icon,
  iconPosition = "left",
  fullWidth = false,
  size = "md",
  disabled = false,
  className = "",
  ...props
}, ref) => {
  const baseClass = "input-wrapper";
  const sizeClass = `input-${size}`;
  const fullWidthClass = fullWidth ? "input-full-width" : "";
  const errorClass = error ? "input-error" : "";
  const disabledClass = disabled ? "input-disabled" : "";
  const iconClass = icon ? `input-with-icon-${iconPosition}` : "";
  const wrapperClasses = [
    baseClass,
    fullWidthClass,
    className
  ].filter(Boolean).join(" ");
  const inputClasses = [
    "input",
    sizeClass,
    errorClass,
    disabledClass,
    iconClass
  ].filter(Boolean).join(" ");
  return (0, import_jsx_runtime4.jsxs)("div", { className: wrapperClasses, children: [
    label && (0, import_jsx_runtime4.jsx)("label", { className: "input-label", children: label }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "input-container", children: [
      icon && iconPosition === "left" && (0, import_jsx_runtime4.jsx)("span", { className: "input-icon input-icon-left", children: icon }),
      (0, import_jsx_runtime4.jsx)(
        "input",
        {
          ref,
          type,
          className: inputClasses,
          disabled,
          ...props
        }
      ),
      icon && iconPosition === "right" && (0, import_jsx_runtime4.jsx)("span", { className: "input-icon input-icon-right", children: icon })
    ] }),
    (error || helperText) && (0, import_jsx_runtime4.jsx)("span", { className: `input-helper ${error ? "input-helper-error" : ""}`, children: error || helperText })
  ] });
});
Input.displayName = "Input";
var Input_default = Input;

// ../shared/src/hooks/useAuth.js
var import_react5 = __toESM(require_react(), 1);
var useAuth = () => {
  const [user, setUser] = (0, import_react5.useState)(null);
  const [token, setToken] = (0, import_react5.useState)(null);
  const [loading, setLoading] = (0, import_react5.useState)(true);
  (0, import_react5.useEffect)(() => {
    const storedToken = localStorage.getItem("authToken");
    const storedUser = localStorage.getItem("user");
    if (storedToken && storedUser) {
      setToken(storedToken);
      setUser(JSON.parse(storedUser));
    }
    setLoading(false);
  }, []);
  const login = (userData, authToken) => {
    localStorage.setItem("authToken", authToken);
    localStorage.setItem("user", JSON.stringify(userData));
    setToken(authToken);
    setUser(userData);
  };
  const logout = () => {
    localStorage.removeItem("authToken");
    localStorage.removeItem("user");
    setToken(null);
    setUser(null);
  };
  const updateUser = (userData) => {
    localStorage.setItem("user", JSON.stringify(userData));
    setUser(userData);
  };
  return {
    user,
    token,
    loading,
    isAuthenticated: !!token,
    login,
    logout,
    updateUser
  };
};

// ../shared/src/hooks/useSocket.js
var import_react6 = __toESM(require_react(), 1);
var useSocket = (url, options = {}) => {
  const [socket, setSocket] = (0, import_react6.useState)(null);
  const [isConnected, setIsConnected] = (0, import_react6.useState)(false);
  const [error, setError] = (0, import_react6.useState)(null);
  const socketRef = (0, import_react6.useRef)(null);
  (0, import_react6.useEffect)(() => {
    socketRef.current = lookup(url, {
      autoConnect: true,
      ...options
    });
    socketRef.current.on("connect", () => {
      setIsConnected(true);
      setError(null);
    });
    socketRef.current.on("disconnect", () => {
      setIsConnected(false);
    });
    socketRef.current.on("connect_error", (err) => {
      setError(err.message);
      setIsConnected(false);
    });
    setSocket(socketRef.current);
    return () => {
      if (socketRef.current) {
        socketRef.current.disconnect();
      }
    };
  }, [url]);
  const emit = (event, data) => {
    if (socketRef.current && isConnected) {
      socketRef.current.emit(event, data);
    }
  };
  const on = (event, handler) => {
    if (socketRef.current) {
      socketRef.current.on(event, handler);
      return () => {
        if (socketRef.current) {
          socketRef.current.off(event, handler);
        }
      };
    }
  };
  return {
    socket,
    isConnected,
    error,
    emit,
    on
  };
};

// ../shared/src/utils/api.js
var createApiClient = (baseURL) => {
  const api = axios_default.create({
    baseURL,
    headers: {
      "Content-Type": "application/json"
    }
  });
  api.interceptors.request.use(
    (config) => {
      const token = localStorage.getItem("authToken");
      if (token) {
        config.headers.Authorization = `Bearer ${token}`;
      }
      return config;
    },
    (error) => {
      return Promise.reject(error);
    }
  );
  api.interceptors.response.use(
    (response) => response,
    (error) => {
      if (error.response?.status === 401) {
        localStorage.removeItem("authToken");
        localStorage.removeItem("user");
        window.location.href = "/login";
      }
      return Promise.reject(error);
    }
  );
  return api;
};
var handleApiError = (error) => {
  if (error.response) {
    return {
      message: error.response.data?.message || "An error occurred",
      status: error.response.status,
      data: error.response.data
    };
  } else if (error.request) {
    return {
      message: "Network error. Please check your connection.",
      status: 0
    };
  } else {
    return {
      message: error.message || "An unexpected error occurred",
      status: -1
    };
  }
};
export {
  Button_default as Button,
  Card_default as Card,
  Input_default as Input,
  Modal_default as Modal,
  createApiClient,
  handleApiError,
  useAuth,
  useSocket
};
//# sourceMappingURL=@resqq_shared.js.map
